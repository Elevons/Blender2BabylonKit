Volumetric lighting needs to be re-implemented
Atmosphere barely tested
Geospatial Camera barely tested
SDF Text not tested
NLA/Animations not tested
Particles are still spawned even if the emitter is set to off
Colliders are still spawned even if the layer is turned off
Maybe remove "apply scale" flag for colliders and constraints and make it automatic
Typing I in the filter view of the scene explorer hides it, C activates collision
large world rendering needs some work
sun away from center leads to some weird shadows
Test reflection probes - done?
collision/render layers need testing
adding/removing components at runtime
autoLOD needs a saving and load generation system (to be implemented with build setup)
Prefabs with cameras not tested, prefabs do not allow reflection prrobes
IsEntityInsideColliderVolume does not use babylon or havok volumes, it uses manifest volumes. This is a bit confusing
Export/Scene tab needs a rendering manager block for settings

